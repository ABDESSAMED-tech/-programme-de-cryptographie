# Abdelmalek LAZLI Project

Welcome to the Abdelmalek LAZLI project! This project is designed to provide a user-friendly interface for various functionalities.

## How to Run the Project

1. **Install Dependencies:**
   Open your command line or terminal and navigate to the project directory. Run the following command to install the required dependencies:

   pip install -r requirements.txt
2. **Run the Project :**
Once the dependencies are installed, you can run the project using the following command:
    python main.py
3. **Interface:**
After executing the command, the app's interface will appear. Simply use it as needed.
4. **Enjoy**

